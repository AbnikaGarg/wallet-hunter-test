import 'package:flutter/material.dart';

class SamajName extends StatelessWidget {
  const SamajName({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Samaj")),
      body: Column(children: [
        
      ],),
    );
  }
}
